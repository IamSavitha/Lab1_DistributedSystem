# ---------------------------
# App Configuration
# ---------------------------
PORT=4000
NODE_ENV=development

# ---------------------------
# Frontend URL (for CORS)
# ---------------------------
FRONTEND_ORIGIN=http://localhost:5173 
#"This should match React app's URL"

# ---------------------------
# MySQL Database Configuration
# ---------------------------
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=Mypassword
DB_NAME=airbnb_lab
#"MySQL server is running locally and the 'airbnb_lab' database exists."

# ---------------------------
# Session Configuration
# ---------------------------
SESSION_SECRET=super_secret_key
SESSION_COOKIE_NAME=sid
SESSION_COOKIE_DAYS=7
# Used for login sessions (cookie-based auth)
# ---------------------------
# Cookie Security Settings
# ---------------------------
COOKIE_SECURE=false
COOKIE_SAMESITE=lax
# (For production, set SECURE=true and SAMESITE=none)

# ---------------------------
# Agent API (FastAPI Concierge)
# ---------------------------
AGENT_API_URL=http://localhost:8001
# Used if backend needs to call the Python agent service
